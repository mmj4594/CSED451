#version 460

in vec3 aPos;
in vec3 aColor;
in vec3 lightColor;

out vec3 color;

uniform mat4 modelView;
uniform mat4 projection;

void main() {
	gl_Position = projection * modelView * vec4(aPos, 1.0);
	color = aColor;
}